{
  "name": "@identik/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "cross-fetch": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^9.16.0",
    "rimraf": "^6.0.1",
    "tslib": "^2.8.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
